<%= form_for(@company) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :registry_number %>
  <%= f.text_field :registry_number, placeholder: "Digite apenas números" %>

  <%= f.label :state_registration %>
  <%= f.text_field :state_registration %>

  <%= f.label :municipal_registration %>
  <%= f.text_field :minicipal_registration %>

  <%= f.check_box :active %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
